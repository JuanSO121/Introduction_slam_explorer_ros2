from setuptools import setup
import os
from glob import glob

package_name = 'tutorial_pkg'

setup(
    name=package_name,
    version='0.0.0',
    packages=[package_name],
    data_files=[
        ('share/ament_index/resource_index/packages',
            ['resource/' + package_name]),
        ('share/' + package_name, ['package.xml']),
        # CORREGIR: Los globs estaban mal formados
        (os.path.join('share', package_name, 'launch'), glob('launch/*.py')),
        (os.path.join('share', package_name, 'config'), glob('config/*.yaml')),
        (os.path.join('share', package_name, 'worlds'), glob('worlds/*.world')),
        # CORREGIR: Incluir todos los archivos del modelo recursivamente
        (os.path.join('share', package_name, 'models', 'salon_model'), 
         glob('models/salon_model/model.config') + 
         glob('models/salon_model/model.sdf')),
        (os.path.join('share', package_name, 'models', 'salon_model', 'meshes'), 
         glob('models/salon_model/meshes/*.dae')),
        (os.path.join('share', package_name, 'rviz'), glob('rviz/*.rviz')),
        # Incluir scripts si existen
        (os.path.join('share', package_name, 'scripts'), glob('scripts/*.py')),
        (os.path.join('share', package_name, 'scripts'), glob('scripts/*.sh')),
    ],
    install_requires=['setuptools'],
    zip_safe=True,
    maintainer='jjso',
    maintainer_email='jjso@todo.todo',
    description='Tutorial package con exploración robusta',
    license='Apache License 2.0',
    tests_require=['pytest'],
    entry_points={
        'console_scripts': [
            'exploration_monitor = tutorial_pkg.exploration_monitor:main',
            'exploration_restarter = tutorial_pkg.exploration_restarter:main',
            'exploration_visualizer = tutorial_pkg.exploration_visualizer:main',
        ],
    },
)